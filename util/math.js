"use strict";

const matmul = require('./matmul'),
    product = require('./product'),
    sum = require('./sum'),
    divide = require('./divide'),
    log = require('./log'),
    max = require('./max'),
    min = require('./min'),
    exp = require('./exp');

module.exports = {
    matmul,
    product,
    sum,
    divide,
    log,
    max,
    min,
    exp
}