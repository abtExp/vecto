"use strict";

const diff = require('./diff'),
    divide = require('./divide'),
    exp = require('./exp'),
    log = require('./log'),
    max = require('./max'),
    min = require('./min'),
    pad = require('./pad'),
    pow = require('./pow'),
    product = require('./product'),
    sqrt = require('./sqrt'),
    sum = require('./sum');

module.exports = {
    diff,
    divide,
    exp,
    log,
    max,
    min,
    pad,
    pow,
    product,
    sqrt,
    sum,
}