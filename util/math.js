"use strict";

const product = require('./product'),
    sum = require('./sum'),
    divide = require('./divide'),
    log = require('./log'),
    max = require('./max'),
    min = require('./min'),
    sqrt = require('./sqrt'),
    pow = require('./pow'),
    exp = require('./exp');

module.exports = {
    product,
    sum,
    divide,
    log,
    max,
    min,
    sqrt,
    pow,
    exp
}